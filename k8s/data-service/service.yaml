apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: cloudnative
  labels:
    app: data-service
    component: producer
    tier: backend
    part-of: devops-u4-cloudnative
spec:
  type: ClusterIP
  selector:
    app: data-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
